{
  "name": "postgrest-typescript",
  "version": "0.0.1-beta.2",
  "description": "Typescript Client for Postgrest",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "start": "node dist/index.js",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.83.0"
  },
  "devDependencies": {
    "@types/node": "^8.0.47",
    "@types/request": "^2.0.8"
  }
}
